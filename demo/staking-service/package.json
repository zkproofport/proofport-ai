{
  "name": "ledger-house-staking",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A staking service open only to KYC-verified people, which never learns which wallet holds the KYC.",
  "scripts": { "start": "node src/server.ts" },
  "dependencies": { "ethers": "^6.16.0", "express": "^4.21.2" }
}
