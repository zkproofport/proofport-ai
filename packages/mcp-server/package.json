{
  "name": "proofport-mcp",
  "version": "0.1.0",
  "description": "Local MCP server for ZK proof generation via ZKProofport. Runs locally, calls remote TEE server for proof generation.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "proofport-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@proofport/client": "*",
    "@coinbase/agentkit": ">=0.2.0",
    "@coinbase/cdp-sdk": ">=0.2.0",
    "zod": "^3.23.0"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
